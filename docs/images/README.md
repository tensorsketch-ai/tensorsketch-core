# Documentation images

Placeholder directory for diagrams referenced by the docs. Add images here and the existing
Markdown references will resolve.

Needed:

- `supersteps.png` — the BSP superstep loop: three phases (plan → execute → barrier) with the
  active set flowing from one step to the next. Referenced by
  [`concepts/execution-model.md`](../concepts/execution-model.md).
